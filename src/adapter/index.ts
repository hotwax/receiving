import { api, client, getConfig, fetchGoodIdentificationTypes, getUserFacilities, getUserPreference, hasError, initialise, logout, resetConfig, updateUrls, updateToken, setUserTimeZone,
  getAvailableTimeZones, getProductIdentificationPref, setProductIdentificationPref, setUserPreference} from '@hotwax/oms-api'

export {
  api,
  client,
  fetchGoodIdentificationTypes,
  getConfig,
  getUserFacilities,
  getProductIdentificationPref,
  getUserPreference,
  hasError,
  initialise,
  logout,
  resetConfig,
  updateUrls,
  updateToken,
  setUserTimeZone,
  setProductIdentificationPref,
  setUserPreference,
  getAvailableTimeZones
}